{
  "name": "webbender",
  "version": "1.0.2",
  "description": "A powerful bookmarklet to bend the web to your will",
  "author": "ilim-cell",
  "license": "MIT",
  "scripts": {
    "build": "cross-env SITE_INDEX_FILE=site/index.html node scripts/build.js",
    "format": "prettier --write \"src/**/*.js\"",
    "format:check": "prettier --check \"src/**/*.js\"",
    "watch": "cross-env SITE_INDEX_FILE=site/index.html node scripts/build.js --watch",
    "test": "npm run test:e2e",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug",
    "prepublishOnly": "npm run format && npm run build"
  },
  "keywords": [
    "bookmarklet",
    "web-editor",
    "browser-extension"
  ],
  "devDependencies": {
    "cross-env": "^7.0.3",
    "prettier": "^3.0.0",
    "terser": "^5.47.1",
    "semantic-release": "^25.0.3",
    "@semantic-release/changelog": "^6.0.0",
    "@semantic-release/git": "^10.0.0",
    "@semantic-release/github": "^9.0.0",
    "@playwright/test": "^1.40.0",
    "commitlint": "^18.0.0",
    "@commitlint/config-conventional": "^21.0.1",
    "@commitlint/cli": "^21.0.1",
    "@axe-core/cli": "^4.8.0",
    "husky": "^9.1.7"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
